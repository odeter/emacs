<template>
  <v-container ma-0 pa-5 bg grid-list-md text-xs-center fluid>
    <v-layout wrap align-center fluid pb-5>
      <v-flex>
        <v-subheader>Report Generator</v-subheader>
        <v-divider></v-divider>
      </v-flex>
    </v-layout>
    <v-row>
      <v-col>
        <v-btn
          class="ma-2"
          :loading="loading"
          :disabled="loading"
          color="primary"
          @click="getreport()"
        >
          Generate pdf
        </v-btn>
      </v-col>
    </v-row>
  </v-container>
</template>

<script>
  import authservice from '@/services/BackendService.js'

  export default {
    data() {
      return {
        loading: false,
        daterange: null
      }
    },
    methods: {
      getreport(){
        this.loading = true
        authservice
          .getreportpdf(daterange)
          .then(result => {

          })
          .catch(error => {
            console.log('Error')
            console.log(error)
          })
      }
    }

  }
</script>
