#!/bin/bash
# A simple script

response=$(curl --compressed https://raw.githubusercontent.com/stamparm/ipsum/master/ipsum.txt 2>/dev/null);

if [ 0 -eq $? ]; then
    ${response} | grep -v "#" | grep -v -E "\s[1-2]$" | cut -f 1 > ip_list.txt && python3 automaltest.py
fi

python3 automaltest.py
